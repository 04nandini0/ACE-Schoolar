zerops:
  - setup: app
    build:
      base: nodejs@20
      buildCommands:
        - npm install
      deployFiles:
        - .
    run:
      base: nodejs@20
      ports:
        - port: 3000
          httpSupport: true
      start: node server.js
